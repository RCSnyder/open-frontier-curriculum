# Machine-readable data

These CSV/JSON files are generated from the spreadsheet migration and mirror the Markdown structure.

For future maintenance, prefer changing Markdown/front matter and updating data in the same PR until a full generator is adopted.

The validator checks key counts and frontier metadata.
