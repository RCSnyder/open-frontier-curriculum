# Archive

`source-workbook.xlsx` is the spreadsheet snapshot from which the first repository version was generated.

After migration, Markdown is intended to become the human-editable source of truth. The workbook is preserved for provenance and comparison.
